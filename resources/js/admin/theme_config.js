import AppConfig from './config.js';

class ThemeConfig {
  constructor() {}

  init = () => {
    this._setThemeColor();
    this._setThemeMode();
    this._changeThemeColor();
    this._changeThemeMode();
    this._resetSetting();
  }

  _setThemeColor = () => {
   // Full source code in paid license
  }

  _setThemeMode = () => {
    // Full source code in paid license
  }

  _changeThemeColor = () => {
    // Full source code in paid license
  }

  _changeThemeMode = () => {
    // Full source code in paid license
  }

  _resetSetting = () => {
    // Full source code in paid license
  }
}

export default ThemeConfig;
